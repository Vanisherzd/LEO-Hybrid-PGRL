> **OBSOLETE legacy note — archived; not current claims.**

# Pre-Hardware Review Snapshot (legacy)

This was a pre-hardware review snapshot package. It referenced a prior target
venue, a compiled legacy paper PDF, and headline numbers that **no longer match**
the current, conservative claim scope, so that content has been removed.

For the current package see:

- `paper/icc_main.tex` — the active conference paper (build: `tectonic paper/icc_main.tex`)
- `README.md` — current Key Results and claim scope
- `data/README.md` — lightweight data registry

Current scope claims **no** measured PER, **no** receiver decoding, **no** packet
delivery validation, **no** CRC validation, **no** full hardware validation, and
**no** Bayesian posterior. The deterministic predictor RMSE is **not** improved by
the uncertainty stages (mean head frozen).
