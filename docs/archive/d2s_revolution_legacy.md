> **OBSOLETE legacy note — archived; not current claims.**

# D2S "Revolution" Narrative (legacy, superseded)

This file held an early promotional narrative for the project. It contained
headline performance figures and framing that **no longer match** the current,
conservative claim scope and have therefore been removed.

For current, evidence-aligned results and scope see:

- `paper/icc_main.tex` — the active conference paper
- `README.md` — Key Results (simulation / offline / IQ-level detection only)

Current scope claims **no** measured PER, **no** receiver decoding, **no** packet
delivery validation, **no** CRC validation, **no** full hardware validation, and
**no** Bayesian posterior. Deterministic position RMSE is **not** improved by the
uncertainty stages (mean head frozen).
