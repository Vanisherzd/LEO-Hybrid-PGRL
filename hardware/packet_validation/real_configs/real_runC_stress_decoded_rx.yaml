# Real conducted decoded-RX run C: stress (higher attenuation / lower SNR /
# injected CFO via controlled conducted means ONLY). No OTA, no power increase.
# Fails preflight until rx_log_path points at a real decoded RX log (expected).
run_id: real_runC_stress
mode: decoded_rx
alpha: 0.25
n_packets: 200
seed: 42
center_frequency_hz: 868000000
tx_power_dbm: 10
sample_rate_sps: 1000000
gain_db: 45
attenuator_db: REPLACE_WITH_STRESS_ATTENUATOR_DB
duration_s: 10
operator: REPLACE_WITH_OPERATOR_NAME
output_dir: validation_runs/real_runC_stress
require_conducted_setup_ack: true
require_no_ota_ack: true
safety:
  conducted_or_shielded: true
  acknowledged_by: REPLACE_WITH_OPERATOR_NAME
tx:
  backend: lr1121_uart
  freq_hz: 868000000
  power_dbm: 10
rx:
  backend: log_parser
  rx_log_path: REPLACE_WITH_REAL_RX_LOG_PATH_FOR_RUN_C
notes: "Stress conducted/shielded decoded-RX run. Use controlled attenuation/CFO only. Do not claim PER until decoded RX log is present."
